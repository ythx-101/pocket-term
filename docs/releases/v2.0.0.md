# Pocket Term v2.0.0 发布说明

- 发布版本：`v2.0.0`
- 发布对象：当前正在运行的 Pocket Term Web
- 发布状态：待完成 Git tag 与最终发布门禁

## 本版本内容

- 手机端会话列表、消息气泡、Tier A/Tier B 消息流和 SSE 更新
- 受保护的 pane 消息发送与只读熔断
- 图片上传和图片预览
- Markdown 附件安全预览
- HTML 附件白名单清洗、sandbox 隔离和独立预览窗口
- 可选 Web Push 通知
- 运行时付费答疑群入口

## 付费答疑群

- 群名：**QingYue skill 答疑群**
- 单价：**55 元**
- 二维码：只作为运行时资源，不提交到 Git 或发布包
- 有效期：以 Web 页面当前显示内容为准

当以下运行时配置全部存在时，Pocket Term 的“我”页面显示入口：

```text
PT2_PAID_GROUP_NAME
PT2_PAID_GROUP_PRICE
PT2_PAID_GROUP_QR_PATH
PT2_PAID_GROUP_EXPIRES
```

二维码路径必须位于文件白名单根目录下，并通过图片扩展名、realpath、symlink 和大小检查。

## 付费方式

入群费用为 **55 元**，推荐使用微信支付。付款前请核对群名 **QingYue skill 答疑群** 和收款信息：

![QingYue skill 答疑群收款二维码](../../assets/qingyue-payment-qr.png)

收款二维码可能因平台规则或有效期变化而更新，以发布页当前版本为准。

## 安全边界

- 默认监听 `127.0.0.1:7690`
- 不把二维码、VAPID 私钥、运行时状态或上传数据提交到 Git
- Markdown 内容不执行原始 HTML、脚本或图片
- HTML 预览使用白名单清洗和空 sandbox
- 上传与文件读取限制扩展名、大小、路径穿越和 symlink escape

## 验证

```bash
node --check server.js
node --check public/app.js
npm test
```

本次发布前验证：

- focused tests：46/46 通过
- 完整 `npm test`：390/394；4 个失败来自 live Herdr/Claude 环境：旧测试依赖的 `w9:p8` 不存在，Claude live 测试因账号订阅被禁用无法返回 PONG
- `scripts/install.sh --dry-run --no-start --no-herdr-integ --port 7690`：通过
- `/herd/api/paid-group`：已启用，返回群名、价格和有效期
- 收款二维码文件已纳入本发布版本；群邀请二维码仍只使用运行时配置

发布后仍需记录完整的 systemd PID、`/herd/api/state`、SPA、SSE 和二维码接口健康结果。

## 回滚

不移动或删除已发布 tag。代码异常时使用独立 Git revert commit，恢复上一已验证版本后，在批准窗口内重启 `pocket-term-2.service`，再重复健康检查。二维码和 `state/`、上传目录不随代码回滚删除。
