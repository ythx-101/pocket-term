{
  "name": "pocket-term-2",
  "version": "0.2.0",
  "private": true,
  "description": "Pocket agent fleet chat client — herdr-backed, WeChat-style (M0 read-only)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/**/*.test.mjs"
  }
}
